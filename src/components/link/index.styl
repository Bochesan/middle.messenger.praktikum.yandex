@import '../../styles/helpers/index.styl';
.link
	font-size: inherit;
	font-weight: inherit;
	color: inherit;
	text-decoration: none;
	transition: 0.3s;

	&:hover
		color: $bg-ultramarine-blue;

	&[disabled]
		cursor: default;
		opacity: 0.2;
		color: inherit;
