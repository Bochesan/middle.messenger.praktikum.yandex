@import '../../styles/helpers/index.styl'
.auth-main
  display grid
  grid-row-gap set-rem(16px)

.auth-header
  display flex
  flex-wrap wrap
  gap set-rem(16px) set-rem(32px)

.auth-form
  display grid
  grid-row-gap set-rem(32px)

.auth-footer
  display grid
  grid-column-gap set-rem(16px)
